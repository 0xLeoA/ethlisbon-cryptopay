const ERC20ABI = require("./ERC20ABI.json")
const chainidtodata = require("./chainidtodata.json")
const cryptopayabi = require("./cryptopayabi.json")

module.exports = {
    ERC20ABI,
    chainidtodata,
    cryptopayabi
}